name: Output Information
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: "Output Github Context"
        #to access the metadata use ${{...}}
        # toJson() is an expression
        run: echo "${{toJson(github)}}"  
 